name: Temporary German Translation Model Export

on:
  pull_request:
    branches: [main]

jobs:
  export-model:
    runs-on: ubuntu-latest
    timeout-minutes: 90
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install model tools
        run: pip install --disable-pip-version-check huggingface_hub
      - name: Download NLLB English to German model files
        run: |
          python - <<'PY'
          from huggingface_hub import snapshot_download
          snapshot_download(
              repo_id='facebook/nllb-200-distilled-600M',
              local_dir='nllb-en-de-model',
              allow_patterns=['*.json','*.safetensors','*.bin','*.model','*.spm','*.txt','tokenizer*'],
          )
          PY
      - name: Upload model
        uses: actions/upload-artifact@v4
        with:
          name: nllb-en-de-model
          path: nllb-en-de-model
          retention-days: 1
